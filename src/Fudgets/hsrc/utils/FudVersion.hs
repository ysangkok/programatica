module FudVersion where

version = version13x

version13x = "version 13x" -- only for documentation, use "version" instead

-- The version number should be increased immediately after a public release.
